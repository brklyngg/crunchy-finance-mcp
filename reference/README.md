# Reference Files

Temporary directory containing source files from other Crunchy Tools repos,
copied here so agents can read them during implementation.

**DELETE THIS DIRECTORY before CRU-55 (package/publish step).**

## Files

- `skill-router.ts` — From `crunchy-sheets/cloud-functions/src/`. Contains skill
  definitions, prompts, and system addenda for CRU-51 tool handler porting.

## Missing (TTYM)

The Talk To Your Money source files (`transaction-classifier.ts`, `financial-statement.ts`,
`financial-metrics.ts`) are NOT on this machine. CRU-52 must be implemented from the
architecture plan spec (Section 3, Tool 5) without source reference.
